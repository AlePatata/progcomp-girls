#text(size: 8pt)[*Vim*]
- `vim .vimrc`

```vim
syntax on
set re=0
set smartindent
set tabstop=2
set shiftwidth=2
set expandtab
set nu
set rnu
```

#text(size: 8pt)[*Bash*]
- `vim .bashrc`
- Tip: reiniciar la terminal, o ejecutar `source .bashrc`

```
alias g++="g++ -o run.out -Wall"
```

